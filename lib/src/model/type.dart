import 'package:animation_transition/animation_transition.dart';

typedef HoverToken = Function(int token);

enum HoverPositionVertical {
  top,
  bottom,
  center,
}

enum HoverPositionHorizontal {
  left,
  right,
  center,
}
