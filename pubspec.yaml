name: hover_float_animation
description: "Create floating widgets for your apps with input animation effects."
version: 0.0.4
homepage: http://nio.ilove-soft.com

environment:
  sdk: ^3.5.3
  flutter: '>=3.3.0'

dependencies:
  animation_transition: ^0.0.8
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0